#include <stdio.h>
#include <ctype.h>
#include <string.h>

#include "./convertion_utils.cpp"

char* infixToPrefix(const char* str) {
	
	int len = strlen(str);
	
	for (int i = 0; i < len; i++) {
		
	}
	
} 

